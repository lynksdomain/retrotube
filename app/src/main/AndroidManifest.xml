<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Intentionally no storage/media permissions: the library is entirely
         Storage Access Framework (SAF) based. Folders are picked via the
         system's own document tree picker, which grants scoped access without
         needing a broad runtime permission. -->

    <application
        android:allowBackup="true"
        android:label="RetroTube"
        android:theme="@style/Theme.RetroTube"
        android:supportsRtl="true">

        <activity
            android:name=".LibraryActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EffectSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.RetroTube.Player" />

    </application>
</manifest>
